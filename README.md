# Projet-web

### 1. Backend (Laravel)
Ouvrir le dossier backend :
1-composer install
2-Créer un fichier `.env` (configurer la DB)
3-php artisan key:generate
4-php artisan migrate
5-php artisan serve

### 2. Frontend (React)
Ouvrir le dossier frontend :
1)npm install
2)npm start